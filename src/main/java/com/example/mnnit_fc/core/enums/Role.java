package com.example.mnnit_fc.core.enums;

public enum Role {
    ROLE_ADMIN,     // Full access (Dashboard, match editing, attendance)
    ROLE_PLAYER     // Read-only access (View stats, schedules, etc.)
}
